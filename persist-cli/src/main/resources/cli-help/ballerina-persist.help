NAME
       Perform operations on Ballerina Persistent Layer

SYNOPSIS
       bal persist <command> [args]

COMMANDS
        The available subcommands are:

   Package Commands:
        init            Initialize the persistence layer in the Ballerina project.
        generate        Generate clients for the defined entities inside the Ballerina project
        push            Generate SQL script for the defined entities and execute the script against the Database.

