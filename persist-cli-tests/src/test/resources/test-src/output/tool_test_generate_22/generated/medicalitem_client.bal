// AUTO-GENERATED FILE. DO NOT MODIFY.

// This file is an auto-generated file by Ballerina persistence layer for MedicalItem.
// It should not be modified by hand.

import ballerinax/mysql;
import ballerina/persist;
import ballerina/sql;
import ballerina/time;

public client class MedicalItemClient {
    *persist:AbstractPersistClient;

    private final string entityName = "MedicalItem";
    private final sql:ParameterizedQuery tableName = `MedicalItem`;

    private final map<persist:FieldMetadata> fieldMetadata = {
        itemId: {columnName: "itemId", 'type: int, autoGenerated: true},
        'string: {columnName: "string", 'type: string},
        'type: {columnName: "type", 'type: string},
        unit: {columnName: "unit", 'type: string},
        "medicalNeed.'record": {'type: int, relation: {entityName: "medicalNeed", refTable: "MedicalNeed", refField: "record"}},
        "medicalNeed.beneficiaryId": {'type: int, relation: {entityName: "medicalNeed", refTable: "MedicalNeed", refField: "beneficiaryId"}},
        "medicalNeed.'time": {'type: time:Civil, relation: {entityName: "medicalNeed", refTable: "MedicalNeed", refField: "time"}},
        "medicalNeed.urgency": {'type: string, relation: {entityName: "medicalNeed", refTable: "MedicalNeed", refField: "urgency"}},
        "medicalNeed.quantity": {'type: int, relation: {entityName: "medicalNeed", refTable: "MedicalNeed", refField: "quantity"}}
    };
    private string[] keyFields = ["itemId"];

    private final map<persist:JoinMetadata> joinMetadata = {medicalNeed: {entity: MedicalNeed, fieldName: "medicalNeed", refTable: "MedicalNeed", refFields: ["itemId"], joinColumns: ["itemId"]}};

    private persist:SQLClient persistClient;

    public function init() returns persist:Error? {
        mysql:Client|sql:Error dbClient = new (host = host, user = user, password = password, database = database, port = port);
        if dbClient is sql:Error {
            return <persist:Error>error(dbClient.message());
        }
        self.persistClient = check new (dbClient, self.entityName, self.tableName, self.keyFields, self.fieldMetadata, self.joinMetadata);
    }

    remote function create(MedicalItem value) returns MedicalItem|persist:Error {
        _ = check self.persistClient.runInsertQuery(value);
        return value;
    }

    remote function readByKey(int key, MedicalItemRelations[] include = []) returns MedicalItem|persist:Error {
        return <MedicalItem>check self.persistClient.runReadByKeyQuery(MedicalItem, key, include);
    }

    remote function read(MedicalItemRelations[] include = []) returns stream<MedicalItem, persist:Error?> {
        stream<anydata, sql:Error?>|persist:Error result = self.persistClient.runReadQuery(MedicalItem, include);
        if result is persist:Error {
            return new stream<MedicalItem, persist:Error?>(new MedicalItemStream((), result));
        } else {
            return new stream<MedicalItem, persist:Error?>(new MedicalItemStream(result));
        }
    }

    remote function update(MedicalItem value) returns persist:Error? {
        _ = check self.persistClient.runUpdateQuery(value);
    }

    remote function delete(MedicalItem value) returns persist:Error? {
        _ = check self.persistClient.runDeleteQuery(value);
    }

    remote function exists(MedicalItem medicalItem) returns boolean|persist:Error {
        MedicalItem|persist:Error result = self->readByKey(medicalItem.itemId);
        if result is MedicalItem {
            return true;
        } else if result is persist:InvalidKeyError {
            return false;
        } else {
            return result;
        }
    }

    public function close() returns persist:Error? {
        return self.persistClient.close();
    }
}

public enum MedicalItemRelations {
    medicalNeed
}

public class MedicalItemStream {

    private stream<anydata, sql:Error?>? anydataStream;
    private persist:Error? err;

    public isolated function init(stream<anydata, sql:Error?>? anydataStream, persist:Error? err = ()) {
        self.anydataStream = anydataStream;
        self.err = err;
    }

    public isolated function next() returns record {|MedicalItem value;|}|persist:Error? {
        if self.err is persist:Error {
            return <persist:Error>self.err;
        } else if self.anydataStream is stream<anydata, sql:Error?> {
            var anydataStream = <stream<anydata, sql:Error?>>self.anydataStream;
            var streamValue = anydataStream.next();
            if streamValue is () {
                return streamValue;
            } else if (streamValue is sql:Error) {
                return <persist:Error>error(streamValue.message());
            } else {
                record {|MedicalItem value;|} nextRecord = {value: <MedicalItem>streamValue.value};
                return nextRecord;
            }
        } else {
            return ();
        }
    }

    public isolated function close() returns persist:Error? {
        if self.anydataStream is stream<anydata, sql:Error?> {
            var anydataStream = <stream<anydata, sql:Error?>>self.anydataStream;
            sql:Error? e = anydataStream.close();
            if e is sql:Error {
                return <persist:Error>error(e.message());
            }
        }
    }
}

