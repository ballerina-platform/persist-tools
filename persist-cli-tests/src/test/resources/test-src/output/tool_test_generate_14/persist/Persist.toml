provider = "mysql"

[persist_generate_14.database]
host = "${clients.host}"
port = "${clients.port}"
user = "${clients.user}"
password = "${clients.password}"
database = "${clients.database}"

[shadowDatabase]

