provider = "mysql"

[database]
host = "${persist_generate_8.clients.host}"
port = "${persist_generate_8.clients.port}"
user = "${persist_generate_8.clients.user}"
password = "${persist_generate_8.clients.password}"
database = "${persist_generate_8.clients.database}"

[shadowDatabase]

