provider = "mysql"

[database]
host = "${persist_generate_22.clients.host}"
port = "${persist_generate_22.clients.port}"
user = "${persist_generate_22.clients.user}"
password = "${persist_generate_22.clients.password}"
database = "${persist_generate_22.clients.database}"
