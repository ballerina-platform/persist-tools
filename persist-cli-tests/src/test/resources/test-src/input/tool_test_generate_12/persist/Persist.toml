provider = "mysql"

[database]
host = "${persist_generate_12.clients.host}"
port = "${persist_generate_12.clients.port}"
user = "${persist_generate_12.clients.user}"
password = "${persist_generate_12.clients.password}"
database = "${persist_generate_12.clients.database}"
