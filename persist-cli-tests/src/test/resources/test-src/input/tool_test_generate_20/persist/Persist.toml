provider = "mysql"

[database]
host = "${persist_generate_20.clients.host}"
port = "${persist_generate_20.clients.port}"
user = "${persist_generate_20.clients.user}"
password = "${persist_generate_20.clients.password}"
database = "${persist_generate_20.clients.database}"
