ignore:
  - "tooling-tests"

fixes:
  "native/src/main/java/io/ballerina/persist/nodegenerator/"

coverage:
  precision: 2
  round: down
  range: "60...80"
  status:
    project:
      default:
        target: 80
